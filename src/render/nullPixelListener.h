#ifndef RT_RENDER_NULL_PIXEL_LISTENER_H_
#define RT_RENDER_NULL_PIXEL_LISTENER_H_

namespace rt { namespace render {
  class NullPixelListener {
    public:
      NullPixelListener();
      ~NullPixelListener();
  };
} }

#endif
