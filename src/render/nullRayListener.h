#ifndef RT_RENDER_NULL_RAY_LISTENER_H_
#define RT_RENDER_NULL_RAY_LISTENER_H_

namespace rt { namespace render {
  class NullRayListener {
    public:
      NullRayListener();
      ~NullRayListener();
  };
} }

#endif
